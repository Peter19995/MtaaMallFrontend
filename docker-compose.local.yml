services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://127.0.0.1:8000/api/v1
    image: mtaamall-frontend-local:latest
    ports:
      - "3000:3000"
    volumes:
      - ./nginx.local.conf:/etc/nginx/conf.d/default.conf:ro
